name: Issue Bot
on: [issue_comment]
jobs:
  issue-bot:
    runs-on: ubuntu-latest
    steps:
    - name: Inspect Issue
      uses: mtrezza/parse-issue-bot@develop
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
