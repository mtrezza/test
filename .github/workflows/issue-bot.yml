name: Issue Bot
on: [issues]
jobs:
  issue-bot:
    runs-on: ubuntu-latest
    steps:
    - name: Inspect Issue
      uses: mtrezza/parse-issue-bot@develop
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}